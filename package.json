{
  "name": "monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "version": "npx lerna version --private"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^9.0.3"
  }
}